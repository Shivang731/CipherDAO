{
  "name": "cipherdao",
  "version": "1.0.0",
  "description": "Private DAO governance on Fhenix CoFHE",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localfhenix",
    "deploy:arbitrum": "hardhat run scripts/deploy.js --network arbitrumSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@fhenixprotocol/cofhe-contracts": "^0.1.0",
    "@fhenixprotocol/cofhe-hardhat-plugin": "^0.1.0"
  }
}